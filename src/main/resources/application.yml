micronaut:
  server:
    port: 8082
  application:
    name: feedbackEngine
cassandra:
  default:
    basic:
      contact-points:
        - "127.0.0.1:9042"
      session-keyspace: feedbackkeyspace
      load-balancing-policy:
        local-datacenter: datacenter1
